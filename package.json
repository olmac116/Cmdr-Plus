{
  "name": "cmdrplus",
  "private": true,
  "version": "1.12.0",
  "description": "Extensible command console for Roblox developers",
  "license": "MIT",
  "scripts": {
    "docs:dev": "moonwave dev --code Cmdrplus",
    "docs:build": "moonwave build --code Cmdrplus",
    "docs:publish": "moonwave build --code Cmdrplus --publish"
  },
  "devDependencies": {
    "moonwave": "^1.1.1"
  }
}
